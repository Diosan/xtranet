- if params[:gallery_id].nil? 
  - gallery = @gallery_image.gallery
- else 
  - gallery = Gallery.find(params[:gallery_id])

= form_for @gallery_image, :html => {:multipart => true} do |f|
  - if @gallery_image.errors.any?
    #error_explanation
      %h2= "#{pluralize(@gallery_image.errors.count, "error")} prohibited this gallery_image from being saved:"
      %ul
        - @gallery_image.errors.full_messages.each do |msg|
          %li= msg
 
  = f.hidden_field :gallery_id, :value => params[:gallery_id]
  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_area :description
  .field
    = f.label :image
    = f.file_field :image
  .field
    = f.label :text
    = f.text_area :text
  .actions
    = f.submit 'Save'
