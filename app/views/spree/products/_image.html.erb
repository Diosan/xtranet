<% if defined?(image) && image %>
  <%= image_tag image.attachment.url(:large), :itemprop => "image", :class => "product_details_image" %>
<% else %>
  <%= product_image(@product, :itemprop => "image", :class => "product_details_image") %>
<% end %>
