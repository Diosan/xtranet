= form_for @promotion_category do |f|
  - if @promotion_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@promotion_category.errors.count, "error")} prohibited this promotion_category from being saved:"
      %ul
        - @promotion_category.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_area :description
  .actions
    = f.submit 'Save'
