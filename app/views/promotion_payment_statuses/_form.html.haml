= form_for @promotion_payment_status do |f|
  - if @promotion_payment_status.errors.any?
    #error_explanation
      %h2= "#{pluralize(@promotion_payment_status.errors.count, "error")} prohibited this promotion_payment_status from being saved:"
      %ul
        - @promotion_payment_status.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_area :description
  .actions
    = f.submit 'Save'
